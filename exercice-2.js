function sentenceSearcher(phrase, mot){
    const séparation = phrase.split(". ");
    const phrasecorrecte = for (let i = 0; i < séparation.length; i++){
        if(séparation[i] == mot{
            return phrasecorrecte;
        })
    };
    return phrasecorrecte 
}

console.log(sentenceSearcher("I have a cat. I have a mat. Things are going swell"));